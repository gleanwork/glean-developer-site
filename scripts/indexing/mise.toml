[tools]
python = "3.12"
uv = "latest"

[tasks.index]
description = "Index the docs into Glean (run pnpm build first)"
run = "uv run glean-idx datasource configure --yes && uv run glean-idx run --yes"

[tasks.sync]
description = "Sync dependencies"
run = "uv sync"
