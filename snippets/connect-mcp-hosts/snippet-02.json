{
  "mcpServers": {
    "glean": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "https://{instance}-be.glean.com/mcp/default",
        "--header",
        "Authorization: Bearer ${GLEAN_API_TOKEN}"
      ]
    }
  }
}
