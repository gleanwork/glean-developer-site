testing:
  cache_dir: .glean_test_cache/
  use_cache: true
  refresh_cache: false
  run_id_prefix: sdk_test
  clients:
    data_client:
      max_items: 5
    comments_client:
      max_items: 20
  negative_test_identities:
    - contractor@example.com
    - external-partners
