# GuestAuthProviderOptions

## Properties

### backend

```ts
backend: string;
```

The search backend server to use. Should match the backend URL for your Glean external search setup.
Example: `https://{your}-be.glean.com/`
