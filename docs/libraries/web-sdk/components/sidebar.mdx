---
title: 'Sidebar Search'
description: 'Integrate contextual search, chat, and recommendations in a convenient sidebar interface'
icon: 'sidebar'
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import { SidebarPreview } from '@site/src/components/WebSdk/previews';
import ComponentDemo from '@site/src/components/WebSdk/ComponentDemo';

:::warning Deprecated
`openSidebar` is marked deprecated in the SDK's type definitions and will be
removed in a future version. For new integrations, consider the
[Recommendations](./recommendations) component for contextual suggestions or
[Modal Search](./modal-search) for on-demand search.
:::

The Glean Sidebar opens a Glean Assistant panel — with chat and search — that slides out from the right side of your page.

<ComponentDemo kind="sidebar">
  <SidebarPreview />
</ComponentDemo>

## Install the SDK

<Tabs groupId="websdk-install">
<TabItem value="npm" label="npm">

```bash snippet=sidebar/snippet-01.sh
npm install @gleanwork/web-sdk
```

</TabItem>
<TabItem value="script" label="Script tag">

Include the JavaScript library in the `<head>` section of your page. Replace `GLEAN_APP_DOMAIN` with your company's Glean web app domain (typically `app.glean.com` or `your-company.glean.com` if you use a custom subdomain).

```html snippet=sidebar/snippet-02.html
<script
  defer
  src="https://{GLEAN_APP_DOMAIN}/embedded-search-latest.min.js"
></script>
```

:::info
The Glean web app domain differs from your company's Glean backend domain
(find yours at [app.glean.com/admin/about-glean](https://app.glean.com/admin/about-glean) under "Server instance (QE)").
The same methods are available on `window.GleanWebSDK` once the `glean:ready` event fires.
:::

</TabItem>
</Tabs>

## Open the sidebar

Call [openSidebar](https://app.glean.com/meta/browser_api/interfaces/GleanWebSDK.html#openSidebar) in response to a user action, such as clicking a button:

```typescript snippet=sidebar/snippet-03.ts
import { openSidebar } from '@gleanwork/web-sdk';

openSidebar({
  backend: 'https://{your}-be.glean.com/',
});
```

The sidebar closes when the user clicks its close button or presses the escape key.

## Options

`openSidebar` accepts [`SearchOptions`](https://app.glean.com/meta/browser_api/interfaces/SearchOptions.html):

| Option              | Type              | Description                                                              |
| ------------------- | ----------------- | ------------------------------------------------------------------------ |
| `query`             | `string`          | If provided, the sidebar opens showing results for this query.           |
| `onSearch`          | `(query) => void` | Invoked when the user performs a search.                                 |
| `datasourcesFilter` | `string[]`        | Restrict results to specific datasources, e.g. `['jira', 'confluence']`. |
| `filters`           | `FilterValue[]`   | Pinned, non-modifiable filters applied to every search.                  |

All components also accept the common [`Options`](https://app.glean.com/meta/browser_api/interfaces/Options.html) — `backend`, `authToken`, `theme`, `themeVariant`, `locale`, and more.
