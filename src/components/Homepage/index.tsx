import React from 'react';
import HomeRedesign from '../home/HomeRedesign';

/** The redesigned homepage (design handoff 1a) — launched, flag removed. */
export default function Home() {
  return <HomeRedesign />;
}
